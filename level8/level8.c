printf("%p, %p \n", var_ab0, var_aac);
fgets(var_s20, 128 , stdin);
if (var_s20 == NULL)
{
	exit(0);
}
i = 5
while (var_s20[i] == "auth "[i] && i>0)
{
	i--;
	dl = "auth "[i] > var_s20[i];
	al = "auth "[i] < var_s20[i];
}
al = al(25) - i;
